# `/api/auth/session` GET API 수정 테스트 체크리스트

## 📋 테스트 준비사항

1. **개발 서버 실행**

   ```bash
   npm run dev
   ```

2. **브라우저 개발자 도구 열기**
   - Chrome/Edge: `F12` 또는 `Cmd+Option+I` (Mac) / `Ctrl+Shift+I` (Windows)
   - Network 탭과 Console 탭 열어두기

---

## ✅ 테스트 항목

### 1. 기본 기능 테스트

#### 1-1. 초기 페이지 로드 시 세션 조회

- [ ] **테스트 방법**
  1. 브라우저를 완전히 닫고 새로 열기 (쿠키 초기화)
  2. `http://localhost:3000` 접속
  3. Network 탭에서 `GET /api/auth/session` 요청 확인

- [ ] **확인 사항**
  - ✅ API 요청이 정상적으로 발생하는지
  - ✅ 응답 상태 코드가 `200`인지
  - ✅ 응답 본문이 `{ user: null }`인지 (로그인 안 된 상태)
  - ✅ Console에 보안 경고가 **없는지** 확인

#### 1-2. 로그인 후 세션 조회

- [ ] **테스트 방법**
  1. 로그인 페이지(`/login`) 접속
  2. 유효한 계정으로 로그인
  3. Network 탭에서 `GET /api/auth/session` 요청 확인

- [ ] **확인 사항**
  - ✅ 로그인 성공 후 `GET /api/auth/session` 요청이 발생하는지
  - ✅ 응답 상태 코드가 `200`인지
  - ✅ 응답 본문에 `user` 객체가 포함되어 있는지
  - ✅ `user.id`와 `user.email`이 정확한지
  - ✅ Console에 보안 경고가 **없는지** 확인

#### 1-3. 홈 페이지 접속 시 세션 조회

- [ ] **테스트 방법**
  1. 로그인된 상태에서 홈 페이지(`/home`) 접속
  2. Network 탭에서 `GET /api/auth/session` 요청 확인

- [ ] **확인 사항**
  - ✅ 페이지 로드 시 `GET /api/auth/session` 요청이 발생하는지
  - ✅ 응답에 정확한 사용자 정보가 포함되는지
  - ✅ Console에 보안 경고가 **없는지** 확인

---

### 2. 보안 경고 확인

#### 2-1. 서버 사이드 보안 경고 해소 확인

- [ ] **테스트 방법**
  1. 개발 서버 터미널 확인
  2. 브라우저에서 여러 페이지 이동하며 API 호출

- [ ] **확인 사항**
  - ✅ 터미널에 다음 경고가 **나오지 않는지** 확인:
    ```
    Using the user object as returned from supabase.auth.getSession()
    or from some supabase.auth.onAuthStateChange() events could be insecure!
    ```
  - ✅ `GET /api/auth/session` 호출 시 경고가 없는지

#### 2-2. 클라이언트 사이드 경고 확인 (참고용)

- [ ] **테스트 방법**
  1. 브라우저 Console 탭 확인
  2. OAuth 로그인 시도 (카카오/구글 등)

- [ ] **확인 사항**
  - ℹ️ 클라이언트 사이드(`oauth-callback`)에서는 경고가 여전히 발생할 수 있음 (정상)
  - ℹ️ 서버 사이드 경고만 해소되었는지 확인

---

### 3. 네트워크 요청 상세 확인

#### 3-1. API 응답 구조 확인

- [ ] **테스트 방법**
  1. Network 탭에서 `GET /api/auth/session` 클릭
  2. Response 탭 확인

- [ ] **확인 사항**
  - ✅ 로그인 안 된 상태: `{ user: null }`
  - ✅ 로그인된 상태: `{ user: { id: "...", email: "..." } }`
  - ✅ 응답 구조가 예상과 일치하는지

#### 3-2. 요청 헤더 확인

- [ ] **테스트 방법**
  1. Network 탭에서 `GET /api/auth/session` 클릭
  2. Headers 탭 확인

- [ ] **확인 사항**
  - ✅ `credentials: include`로 쿠키가 포함되는지
  - ✅ Supabase 관련 쿠키(`sb-*`)가 전송되는지

---

### 4. 토큰 갱신 기능 확인

#### 4-1. 토큰 갱신 동작 확인

- [ ] **테스트 방법**
  1. 로그인된 상태 유지
  2. 1분 이상 대기 (AuthProvider가 주기적으로 호출)
  3. Network 탭에서 주기적인 `GET /api/auth/session` 요청 확인

- [ ] **확인 사항**
  - ✅ 1분마다 `GET /api/auth/session` 요청이 발생하는지
  - ✅ 토큰이 만료되어도 자동으로 갱신되는지
  - ✅ 갱신 후에도 정상적으로 사용자 정보를 반환하는지

---

### 5. 에러 케이스 테스트

#### 5-1. 세션이 없는 상태에서 조회

- [ ] **테스트 방법**
  1. 브라우저 쿠키 삭제 (개발자 도구 > Application > Cookies > 모두 삭제)
  2. 페이지 새로고침
  3. Network 탭에서 `GET /api/auth/session` 요청 확인

- [ ] **확인 사항**
  - ✅ API 요청이 정상적으로 처리되는지
  - ✅ 응답이 `{ user: null }`로 반환되는지
  - ✅ 에러가 발생하지 않는지
  - ✅ Console에 에러가 없는지

#### 5-2. 만료된 토큰 처리

- [ ] **테스트 방법**
  1. 로그인된 상태에서 토큰 만료 시뮬레이션 (Supabase 대시보드에서 토큰 만료 설정)
  2. 또는 토큰을 수동으로 만료시키기
  3. `GET /api/auth/session` 호출

- [ ] **확인 사항**
  - ✅ 만료된 토큰이 자동으로 갱신되는지
  - ✅ 갱신 후 정상적으로 사용자 정보를 반환하는지

---

### 6. 통합 테스트

#### 6-1. 로그인 플로우 전체 테스트

- [ ] **테스트 방법**
  1. 로그아웃 상태에서 시작
  2. 로그인 페이지 접속
  3. 로그인 수행
  4. 홈 페이지로 리다이렉트
  5. 여러 페이지 이동

- [ ] **확인 사항**
  - ✅ 로그인 후 세션 조회가 정상적으로 작동하는지
  - ✅ 사용자 정보가 정확하게 표시되는지
  - ✅ 페이지 이동 시 세션 유지되는지
  - ✅ Console에 보안 경고가 없는지

#### 6-2. 로그아웃 플로우 테스트

- [ ] **테스트 방법**
  1. 로그인된 상태에서 로그아웃 수행
  2. Network 탭에서 `DELETE /api/auth/session` 확인
  3. 이후 `GET /api/auth/session` 호출 확인

- [ ] **확인 사항**
  - ✅ 로그아웃 후 `GET /api/auth/session`이 `{ user: null }`을 반환하는지
  - ✅ 로그인 페이지로 정상적으로 리다이렉트되는지

#### 6-3. 회원가입 플로우 테스트

- [ ] **테스트 방법**
  1. 회원가입 페이지 접속
  2. 새 계정으로 회원가입
  3. Network 탭에서 `GET /api/auth/session` 요청 확인

- [ ] **확인 사항**
  - ✅ 회원가입 후 세션 조회가 정상적으로 작동하는지
  - ✅ 새로 생성된 사용자 정보가 정확하게 반환되는지

---

### 7. 성능 확인

#### 7-1. API 응답 시간 확인

- [ ] **테스트 방법**
  1. Network 탭에서 `GET /api/auth/session` 클릭
  2. Timing 탭 확인

- [ ] **확인 사항**
  - ✅ 응답 시간이 합리적인 범위 내인지 (보통 100-500ms)
  - ✅ `getUser()` + `getSession()` 호출로 인한 성능 저하가 없는지

---

## 🎯 핵심 확인 사항 요약

### ✅ 반드시 확인해야 할 항목

1. **보안 경고 해소**
   - [ ] 개발 서버 터미널에 보안 경고가 **나오지 않음**
   - [ ] `GET /api/auth/session` 호출 시 경고 없음

2. **기능 정상 동작**
   - [ ] 로그인 안 된 상태: `{ user: null }` 반환
   - [ ] 로그인된 상태: 정확한 사용자 정보 반환
   - [ ] 토큰 갱신이 정상적으로 작동

3. **에러 없음**
   - [ ] Console에 에러 없음
   - [ ] Network 탭에 실패한 요청 없음

---

## 📝 테스트 결과 기록

### 테스트 환경

- 브라우저: **\*\***\_\_\_**\*\***
- 날짜: **\*\***\_\_\_**\*\***
- 테스터: **\*\***\_\_\_**\*\***

### 테스트 결과

- [ ] 모든 테스트 통과
- [ ] 일부 테스트 실패 (상세 내용 기록)

### 발견된 이슈

1. ***
2. ***

---

## 💡 참고사항

- 클라이언트 사이드(`oauth-callback`)의 경고는 현재 구조상 유지해도 문제없음
- 서버 사이드 경고만 해소되었는지 확인하면 됨
- 토큰 갱신은 `getSession()` 호출로 자동 처리됨
